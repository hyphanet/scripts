# Sample script configuration file
# Should be saved to ~/.freenetrc
# Scripts will use the default if a variable is not specified here.

# Path to Fred repository
fredDir="/path/to/fred-staging"

# Build target directory
releaseDir="/path/to/FreenetReleased"

# Path to freenet-ext.
freenetExtPath="/path/to/freenet-ext.jar"

# Path to the Windows installer repository.
wininstallerDir="/path/to/wininstaller-staging"

# User to switch to when signing. Does not switch if empty.
signingUser=""

# User to build Windows executables as.
wineUser="freenet-buildwin"

